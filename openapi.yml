openapi: "3.0.3"
info:
  contact:
    name: Sample orders Contact data
    url: 'https://developer.mazinger.com'
    email: developer@mazinger.com
  version: v0.0.1
servers:
  - url1: 'https://localhost:3000'
paths:
  /foo:
    $ref: foo-path.yml
components:
  schemas:
    Foo:
      $ref: Foo.yml
    Foo2:
      $ref: Foo2.yml
    Bar:
      $ref: Bar.yml
